# Variables de Entorno - Local
# Copia este archivo a .env.local y ajusta según tu entorno local
# .env.local NO se debe commitear a git (ya está en .gitignore)

# URL base del backend (sin /api)
VITE_API_BASE_URL=http://localhost:3000

# URL para Socket.io (normalmente la misma que el backend)
VITE_SOCKET_URL=http://localhost:3000
